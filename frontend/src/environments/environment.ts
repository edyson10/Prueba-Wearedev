/**
 * Entorno usado por "ng serve" (desarrollo local).
 * apiUrl apunta directamente al backend Express corriendo en el puerto 3000.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
};
